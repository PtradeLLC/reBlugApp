
function classNames(...classes) {
    return classes.filter(Boolean).join(' ')
}

export default function SingleBlogger() {
    return (
        <div className="bg-white">
            <main>
                {/* Hero section */}

            </main>
        </div>
    )
}
