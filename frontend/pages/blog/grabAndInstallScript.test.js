const GrabAndInstallScript = require('../grabAndInstallScript');

describe('GrabAndInstallScript', () => {
  let grabAndInstallScript;

  beforeEach(() => {
    grabAndInstallScript = new GrabAndInstallScript();
  });

  test('grabScript method should grab the script from the app users', () => {
    // Test implementation here
  });

  test('installScript method should install the grabbed script on the external website', () => {
    // Test implementation here
  });
});
