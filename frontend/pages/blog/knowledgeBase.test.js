const KnowledgeBase = require('../knowledgeBase');

describe('KnowledgeBase', () => {
  let knowledgeBase;

  beforeEach(() => {
    knowledgeBase = new KnowledgeBase();
  });

  test('storeContent method should store the given content as an embedding in the knowledgeBase directory', () => {
    // Test implementation here
  });
});
