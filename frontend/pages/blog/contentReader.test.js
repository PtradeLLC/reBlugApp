const ContentReader = require('../contentReader');

describe('ContentReader', () => {
  let contentReader;

  beforeEach(() => {
    contentReader = new ContentReader();
  });

  test('readContent method should read the content of the given URL and return it', () => {
    // Test implementation here
  });
});
